language: java
jdk:
 - oraclejdk8

before_install:
 - chmod +x mvnw
 - chmod +x update-download.sh
after_success:
 - ls
 - pwd
 - update-download.sh
 # notify Docker Hub to make a new build
 #- >
 # [ "$TRAVIS_BRANCH" == "master" ]
 # && curl -X POST -H "Content-Type: application/json"
 # --data '{"docker_tag_name": "latest"}'
 # https://registry.hub.docker.com/u/eduardopax/web-demo/trigger/$DOCKER_HUB_TOKEN/